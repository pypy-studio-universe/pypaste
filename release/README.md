# Release Artifacts

Generated `.app`, `.zip`, `.dmg`, and `.xcarchive` artifacts belong in this directory locally,
but are intentionally ignored by Git. Publish distributable artifacts through GitHub Releases.

Release notes and checksum manifests are tracked so every published artifact can be verified.
